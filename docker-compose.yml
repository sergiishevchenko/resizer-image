version: '3.8'
    
services:
  web:
    build: .
    command: bash -c "python manage.py migrate && python manage.py runserver"
    volumes:
      - ./test_project/:/usr/src/test_project/
    ports:
      - "8000:8000"
    